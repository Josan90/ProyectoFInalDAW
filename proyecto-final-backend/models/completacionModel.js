const db = require('../config/database');

// No es necesario definir el modelo si estás ejecutando consultas directas

module.exports = {
  // Puedes definir funciones para interactuar con la tabla completacion si es necesario
};
